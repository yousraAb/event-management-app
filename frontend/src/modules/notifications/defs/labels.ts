import { CrudLabels } from '@common/defs/types';

const NotificationLabels: CrudLabels = {
  CreateNewOne: 'Create Notification',
  NewOne: 'notification.new_one',
  ReadAll: 'notification.list',
  Items: 'notification.items',
  EditOne: 'notification.edit_one',
};

export default NotificationLabels;
